# OpenAI / LLM
OPENAI_API_KEY=

# MCP (optional: URL when using HTTP/SSE transport)
MCP_SERVER_URL=http://mcp:8000

# LangGraph API (when running the graph service separately)
LANGGRAPH_API_URL=http://langgraph:8001

# App
STREAMLIT_SERVER_PORT=8501
LOG_LEVEL=INFO
